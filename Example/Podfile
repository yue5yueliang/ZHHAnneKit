use_frameworks!

platform :ios, '10.0'

##忽略.cocoapods中多个specs源引起的警告问题
install! 'cocoapods', :warn_for_unused_master_specs_repo => false

target 'ZHHAnneKit_Example' do
  pod 'ZHHAnneKit', :path => '../'

  target 'ZHHAnneKit_Tests' do
    inherit! :search_paths

    
  end
end
